handlers[ACTION_{{constantCase action}}] = function (state, action){
  const newState = {...state};

  return newState;
};

handlers[ACTION_{{constantCase action}}_SUCCESS] = function (state, action){
  const newState = {...state};

  return newState;
};

handlers[ACTION_{{constantCase action}}_FAILURE] = function (state, action){
  const newState = {...state};

  return newState;
};

